<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Posts</h1>
    <div class="post">
       <% for(post of posts){ %> 
            <h3 class="username">@<%= post.username %></h3>
            <h4 class="content"><%= post.content %></h4>
            <a href="http://localhost:8080/posts/<%= post.id %>">Views Post </a> &nbsp;&nbsp;&nbsp;&nbsp;
            <a href="http://localhost:8080/posts/<%= post.id %>/edit"><span>Edit</span> </a> <br><br>
            <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
                <button>Delete</button>
            </form>
        <% } %>
        <br>
        <b><a href="http://localhost:8080/posts/new" style="font-weight: bolder; ">Create New Post</a></b>
    </div>
    
    
</body>
</html>